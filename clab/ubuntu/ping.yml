---
- hosts: linux
  vars:
    ansible_user: clab
    ansible_password: clab
    ansible_host_key_checking: false
  gather_facts: false
  tasks:
    - name: Check connectivity
      ansible.builtin.ping:
